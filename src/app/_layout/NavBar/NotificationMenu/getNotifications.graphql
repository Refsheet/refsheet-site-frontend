#import '@refsheet/graph/fragments/NotificationsFields.graphql'

query getNotifications {
    getNotifications {
        unreadCount
        notifications {
            ...NotificationsFields
        }
    }
}
